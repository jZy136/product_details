@model IEnumerable<product_details.p_od>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.p_dt.p_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.o_num)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.p_dt.p_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.o_num)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.o_id }) |
                @Html.ActionLink("Details", "Details", new { id = item.o_id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.o_id })
            </td>
        </tr>
    }

</table>
